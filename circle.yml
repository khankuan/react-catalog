machine:
  node:
    version: 6.9.1
deployment:
  release:
    branch: master
    commands:
      - git config --global user.email "khankuan@gmail.com"
      - git config --global user.name "khankuan"
      - npm run release
